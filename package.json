{
  "name": "lolly-claude-plugin",
  "version": "0.0.1",
  "private": true,
  "description": "Claude cowork plugin packaging the Lolly ERP agent as 4 semantic MCP tools with envelope-validated intent-vs-output judging.",
  "license": "MIT",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "packageManager": "pnpm@9.12.0",
  "engines": { "node": ">=22" }
}
