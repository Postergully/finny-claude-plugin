{
  "mcpServers": {
    "lolly": {
      "command": "node",
      "args": ["/Applications/nanoclaw/lolly-claude-plugin/bridge/dist/index.js"],
      "env": {
        "OPENCLAW_URL": "http://127.0.0.1:18789",
        "OPENCLAW_GATEWAY_TOKEN": "${env:LOLLY_GATEWAY_TOKEN}",
        "OPENCLAW_MODEL": "openclaw"
      }
    }
  }
}
